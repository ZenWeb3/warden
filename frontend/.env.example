# Gemini powers the agent's reasoning. Without a key, the app falls back to the
# deterministic engine (demo still works). Direct browser calls expose the key —
# fine for a hackathon demo; proxy it in production.
VITE_GEMINI_API_KEY=your_gemini_api_key
VITE_GEMINI_MODEL=gemini-2.5-flash
