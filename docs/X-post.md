# X showcase tweet (draft)

Requirement: one tweet tagging @BOTChain_ai, including project name + what it solves
+ how it uses BOT Chain + demo + repo + track. Style: lowercase, no em dashes.

---

warden // a gasless, policy-gated agent firewall on @BOTChain_ai

an autonomous agent proposes trades. a 7-policy firewall screens every action
on-chain before it can sign. approved actions execute gasless via the paymaster.
try to jailbreak it, watch the signature get withheld.

track: ai agent
demo: <VIDEO_LINK>
repo: <GITHUB_LINK>
contract: <GUARD_ADDRESS> on scan.bohr.life

built with vibe coding + claude. #BOTChain #AIAgent
