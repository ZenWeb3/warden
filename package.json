{
  "name": "warden",
  "version": "0.1.0",
  "private": true,
  "description": "Warden — a gasless, policy-gated agent firewall on BOT Chain",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "contracts:compile": "cd contracts && npm run compile",
    "contracts:test": "cd contracts && npm run test",
    "contracts:deploy": "cd contracts && npm run deploy",
    "contracts:screen": "cd contracts && npm run screen"
  }
}
